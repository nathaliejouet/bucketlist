{% extends ('base.html.twig') %}

{% block nav %}
    {% if app.user %}
        <div>
            <a href="{{ path('app_logout') }}" title="Deconnection">Logout </a>
            <li><a href="{{ path('wish_add') }}" title="Add an idea">Add an idea</a></li>
        </div>
    {% else %}
        <div>
            <a href="{{ path('app_register') }}" title="Registration">Registration </a> ||
            <a href="{{ path('app_login') }}" title="Login">Login </a>
        </div>
        <br>
    {% endif %}
    <div>
        <li><a href="{{ path('wish_list') }}" title="Wish list">Idea list</a></li>
        <li><a href="{{ path('main_about-us') }}" title="About us">About us</a></li>
    </div>
{% endblock %}
