{% extends ('base.html.twig') %}
{% block pageName %} Detail {% endblock %}
{% block nav %}
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="navbar-collapse collapse w-100 order-3 dual-collapse2">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('main_home') }}" title="GO back home">Home </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('wish_list') }}" title="GO back list">Idea List </a>
                </li>
            </ul>
        </div>
    </nav>
{% endblock %}
            {% block body %}
                {{ parent() }} <br>
                <div class=container-fluid>
                    <h2>{{ record.title }}</h2>
                    <p>{{ record.author }}</p>
                    <p>{{ record.category.name }}</p>
                    <p>{{ record.description }}</p>
                    <p>{{ record.dateCreated|date('Y-m-d') }}</p>
                </div>
            {% endblock %}

